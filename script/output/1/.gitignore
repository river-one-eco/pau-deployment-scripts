# Real run outputs (script/DeployPAU.s.sol writes deploy-pau-latest.json) are generated and must
# not be committed. Keep the directory, the committed schema example, and this file itself.
*
!.gitignore
!deploy-pau-example.json
